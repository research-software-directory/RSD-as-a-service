version: "3.0"

services:
  data-migration:
    container_name: data-migration
    build:
      context: ../
      dockerfile: ./data-migration/Dockerfile
    image: rsd/data-migration:0.0.11
    env_file:
      # use main env file
      - ../.env
    network_mode: "host"
